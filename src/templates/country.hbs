{{#each this}}
<div>
    <img src="{{flags.svg}}" alt="" width="120px" height="120px">
    <h1>{{name.official}}</h1>
    <p>Capital: {{capital}}</p>
    <p>Population: {{population}}</p>
    <p>Languages: {{#each languages}} {{this}} {{/each}}</p>
</div>
{{/each}}